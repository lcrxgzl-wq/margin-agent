name: ci

on:
  push:
    branches: [master, main]
  pull_request:

jobs:
  gates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: corepack enable
      - run: pnpm install --frozen-lockfile
      # workspace 包互相经 dist 入口解析，测试前必须先构建
      - run: pnpm build
      - run: pnpm test
      - run: pnpm typecheck
      - run: pnpm gate:release
